map $http_upgrade $connection_upgrade {
    default upgrade;
    "" close;
}

server {
    listen 80 default_server;
    server_name _ localhost 127.0.0.1;
    resolver 127.0.0.11 ipv6=off valid=10s;
    resolver_timeout 5s;

    client_max_body_size 25m;

    location / {
        set $alento_upstream alento-app:4000;
        proxy_pass http://$alento_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

server {
    listen 80;
    server_name sistema.institutoalento.ong.br;
    resolver 127.0.0.11 ipv6=off valid=10s;
    resolver_timeout 5s;

    client_max_body_size 25m;

    location / {
        set $alento_upstream alento-app:4000;
        proxy_pass http://$alento_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

server {
    listen 80;
    server_name glpi.institutoalento.ong.br;
    resolver 127.0.0.11 ipv6=off valid=10s;
    resolver_timeout 5s;

    client_max_body_size 25m;

    location / {
        set $helpdesk_upstream glpi-origin:80;
        proxy_pass http://$helpdesk_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}

server {
    listen 80;
    server_name hdi.institutoalento.ong.br;
    resolver 127.0.0.11 ipv6=off valid=10s;
    resolver_timeout 5s;

    client_max_body_size 25m;

    location / {
        set $hdi_upstream hdi-origin:80;
        proxy_pass http://$hdi_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }
}
